# ポートフォリオ改修ガイド ／ 引き継ぎ書

『Poro』ポートフォリオサイトを改修・更新する際の指示書。
**AIプログラマーに作業を依頼する場合、この文書を毎回添付してください。**

最終更新：2026-09-05

---

## 0. この文書の使い方

| 読む人 | 読むべき章 |
|---|---|
| **サイト所有者（依頼する側）** | 第6章のプロンプト集 → 第7章のチェック方法 |
| **AIプログラマー（作業する側）** | 第1〜5章を必ず読んでから着手 |

作業を依頼するときは、**この文書＋対象ファイル**を添付し、第6章のテンプレートを使ってください。
「いい感じにして」「モダンにして」は絶対に使わないでください（理由は第8章）。

---

## 1. このサイトは何のためにあるか

**自分がどんな人・作り手かを知ってもらい、覚えてもらうことが第一の目的。作品はその人柄・考え方・制作力を伝える判断材料とする。**

作品の展示場ではありません。芸術作品でもありません。

### 目的の優先順位（迷ったらこの順で決める）

1. **自分の名前・人柄・制作への思いを覚えてもらうこと**
2. **相手が求める情報に速く到達できること**
3. **世界観が美しいこと**

**3が1を邪魔した瞬間、この改修は失敗です。**
「かっこよくなったが情報が見つけにくくなった」は、目的を達成していません。

### ターゲット

| 属性 | 内容 |
|---|---|
| 誰 | ゲーム会社の採用担当・現場のプランナー・リーダー層、専門学校の講師 |
| 年齢層 | 25〜45歳。中心は30代 |
| 状況 | 多数のポートフォリオを短時間で処理している。**初回の判断は10秒未満** |
| 環境 | PCが中心。移動中にスマートフォンで開く場合もある |

### 与えたい印象 / 避けたい印象

| ◎ 目指す | × 避ける |
|---|---|
| お、面白い子が来たな | やばいやつだ…… |
| 判断できる人間だ | 感覚だけの人だ |

---

## 2. コンセプト（変更したら全部やり直し）

> 2026-09-06更新：芯（下記）を「不完全な情報の中で選択させる」から
> **「プレイヤーの心に残る感情を設計する」**へ変更した。記号・皮（ブラウン・
> 深夜紺+琥珀+端末緑・等幅書体・深夜の端末という世界観）は**変更していない**。
> 名前も「Poro」のまま。変わったのは、各コンテンツの文章が拠って立つ
> 論拠（芯）だけ。既存ページの本文は、この新しい芯に合わせて今後
> 書き直していく（第11章 #10）。

### 芯

> **ゲームの中身は忘れても、そのとき感じた気持ちは消えない。**
> **だから「何を感じてほしいか」から設計する。**

サイト所有者は、子どもの頃に遊んだゲームやアニメの「面白かった」「怖かった」という
気持ちが、内容を忘れた後もずっと心に残り続けていることに気づいた人物です。
企画を作る中で「作り手として一番うれしいのはどんなゲームか」を考えたとき、
行き着いたのが「プレイヤーの心に残るゲームを作ること」でした。
制作プロセスは一貫してこの順番で進みます。

1. **コンセプト**：プレイヤーに何を感じてほしいか（面白い／怖い／安心 など）を決める
2. **システム**：その感情のために、どんな仕組みが要るかを設計する
3. **ターゲット**：誰にその感情を届けたいかを考える

| セクション | 芯の現れ方 |
|---|---|
| 企画書 | プレイヤーに何を感じてほしくて、どうシステムに落としたかを設計している |
| ゲーム分析 | 他人の作品が何を感じさせようとしていて、成功／失敗しているかを問うている |
| ボツ企画 | 狙った感情が、なぜ実際には生まれなかったかを記録している |
| プロフィール | 「心に残ったゲーム体験」への気づきが、そのまま設計思想になっている |

麻雀・競馬・ダーツの話は、この芯そのものからは外した。本人が触れたい場合は
プロフィールの趣味欄などに残す想定（第11章 #10）。

### 記号（3つ固定・全媒体で守る／変更なし）

1. **ブラウン**（CRT頭の案内プログラム）
2. **深夜紺＋水色＋端末緑**
3. **等幅書体**

### 皮（世界観／変更なし）

深夜、誰かの古い端末に接続した。ブラウンはそこに常駐する案内プログラム。
ブラウンは所有者の分身ではなく、**所有者が作ったもの**という設定です。

> 記号・皮を変える案（「残心」への改名、墨と朱の配色、ブラウンを落款印にする等）も
> 検討したが、変更範囲が大きくなりすぎるため見送った。芯の変更だけに留める。

---

## 3. 判断の根拠（なぜこの設計なのか）

改修時に「この制約は不要では」と思ったら、ここを読んでください。
**すべての制約に実験的な根拠があります。**

### 3.1 なぜ初期表示2秒・ファーストビュー厳守なのか

- NN/g：ユーザーは**10〜20秒でページを離脱**する。**約30秒**を越えて初めて離脱カーブが平坦になる
- NN/g：平均的なページ訪問で読まれる語数は**多くて28%、実際は20%程度**
- Ladders社アイトラッキング調査(2018)：採用担当の初回スクリーニングは**平均7.4秒**。
  評価されたのはシンプルなレイアウト・明確な見出し・箇条書き

→ **10秒以内に何者かが伝わり、30秒以内に企画書1枚目が画面に出ている**必要がある

### 3.2 なぜ視覚的複雑性を上げてはいけないのか

Tuchら(2012)：119サイトを視覚的複雑性（低・中・高）と原型性（低・高）で分類し、
50ms・500ms・1000msで呈示して美的評価を測定。
**視覚的複雑性が高いサイトは中程度や低いサイトより第一印象が悪くなり、
原型的なサイトは非原型的なサイトより良い第一印象を生む。**
第2研究では17msまで短縮しても両要因が影響していた。

→ **要素を足す改修は、原則として品質を下げる**

### 3.3 なぜ世界観は入口だけで、ナビは標準的なのか

Silvennoinenら(2025, IJHCI)：Webサイト108名12サイトで検証。新規性・典型性の双方が
美的魅力を有意に予測し、新規性の標準化推定値(.857)は典型性(.250)より大きい。
ただし著者らの実務的結論は、**標準的構造から逸脱したサイトでは典型性の役割がいっそう重要になり、
非慣習的なデザインの中に馴染みのある要素を組み込むべき**というもの。

→ **判断の一行ルール**

> **凝ったビジュアル × 標準的なナビゲーション ＝ 面白い子**
> **凝ったビジュアル × 独自のナビゲーション ＝ やばい奴**

### 3.4 なぜ差別化点を1つに絞るのか

フォン・レストルフ効果（孤立効果）：同質な刺激が並ぶ中で、**1つだけ異質なもの**が想起されやすい。
全部を奇抜にすると異質さが消える。

→ 差別化点は**ブラウン1点**に集約する

### 3.5 なぜ凝った実装を避けるのか

AAA採用・GD管理15年の実務者の証言：
創造性を示そうと車輪を再発明した過剰なポートフォリオは、
**95%のケースで採用担当とコンテンツの間に不要な摩擦を生み、相手を興ざめさせる**。
失敗例は長すぎるアニメーション、分かりにくいナビゲーション、曖昧な情報設計、読み込みの重いサイト。
一方、**ブラウザで動くものへの直リンク、PDFへの直リンク、埋め込み動画は「即勝ち」**。

→ **摩擦の除去が最優先**。DLを要求しない、登録を要求しない、待たせない

### 3.6 なぜ数字が入っているのか

日本のプランナー採用では、企業は「会社の利益になる企画か」を重視し、
**ユーザー獲得数・離脱率・課金率などの数字やデータ**で説明すると説得力が増すとされる。
また**他者から受けた評価とその改善点**を盛り込むとPDCAを回せる人材だと示せる。

→ **本文中の実測値・人数・秒数を勝手に削除しないこと**

---

## 4. 現状のファイル構成

> 2026-09-05追記：GitHub Pagesでの公開に合わせて、公開対象のファイルを
> すべて `docs/` フォルダの中に移動した（第10章）。リンクは相対パスのままなので、
> フォルダ内の位置関係が変わらない限り動作に影響はない。

```
docs/                        公開対象（GitHub Pagesはここを配信する）
├── index.html                トップページ（着地点）
├── proposal-001.html         企画書 要約ページ
├── analysis-001.html         ゲーム分析 個別ページ
├── recovered.html            ボツ企画 一覧（書庫表現）
├── recovered-001.html        ボツ企画 個別ページ
├── works.html                作ったゲーム
├── profile.html               プロフィール・連絡先
├── faq.html                   よくある質問（選択式）
├── license.html                ライセンス表記
├── style.css                  全ページ共通スタイル
├── app.js                     全ページ共通スクリプト
└── assets/img, assets/pdf     アップロードした画像・PDF
_edit-tool/                  ローカル編集ツール本体（公開されない。第9章）
編集ツールを起動.bat          編集ツールの起動用
```

### 技術条件

| 項目 | 内容 |
|---|---|
| 種別 | 静的サイト。バックエンド・DB・ログインなし |
| 技術 | HTML + CSS + 素のJavaScript。**フレームワーク・ビルドツールなし** |
| リンク | すべて相対パス（`file://` でも動作する） |
| 外部通信 | Google Fonts のみ |
| 保存領域 | **localStorage / sessionStorage / Cookie 一切不使用** |
| 解析タグ | **なし。訪問者の情報を取得しない** |

### 意図的に採用しなかった構成

**コンテンツのJSON外部化**：`fetch` はローカル確認時にCORSで動かず、
読み込み完了までコンテンツが表示されないため初期表示予算に不利。
10ページ規模ではHTML直書きのほうが速く確実。
**ページ数が大幅に増えるまで、この方針を変更しないこと。**

> 2026-09-05追記：サイト所有者がコードに触れずに内容を追加・編集できるよう、
> `_edit-tool/` にローカル専用の編集ツールを追加した（第9章）。このツールは
> 所有者のPC上でのみ動く下書き管理の仕組みであり、**訪問者が実際に受け取る
> 公開ファイルは今まで通り完全な静的HTML**（fetchなし、JSON読み込みなし）
> のまま生成される。上記の「JSON外部化を採用しない」方針は、あくまで
> **訪問者のブラウザ側の実行時動作**についての方針であり、所有者の手元だけで
> 完結するこの編集ツールの内部データ形式とは矛盾しない。

---

## 5. 変更禁止事項

### 5.1 カラー（6値・変更禁止）

> 2026-09-06更新：本人の声の色を琥珀(#E3A44A)から水色(#4FB8DE)に変更した。
> CSS変数名は `--amber` / `--amber-hi` のまま(値だけ差し替え)。今後名前も
> 揃える場合は `--amber`→`--aqua` のようなリネームが必要（変数名を検索して
> 使用箇所すべてを一括置換すること）。

```css
--navy    #0E1626    背景
--panel   #16223A    カード・ボタン面
--line    #2B3A5C    枠線・区切り線（文字には使わない）
--amber   #4FB8DE    本人の声・見出し・リンク・現在位置（変数名は歴史的経緯でamberのまま）
--term    #7FE3A8    端末側の発話
--fg      #E8EAF0    本文
--muted   #9AA6BF    注釈・日付・非活性
```

強調が必要な場合のみ `--amber-hi (#7ACDEA)` を使用。
**それ以外の有彩色を追加しない。**

高コントラスト表示用に `#FFFFFF` `#C9D2E6` `#4A5F8C` `#33456B` が
`:root.hc` 内で定義されているが、これは設定ON時のみ適用される。**通常表示では使わない。**

#### 色＝話者ルール（このサイト最大の設計思想）

| 色 | 話者 | 使用箇所 |
|---|---|---|
| 水色 | **本人** | 見出し、主張、リンク、現在位置 |
| 端末緑 | **端末側** | 行頭に `>` または `C:\>` が付く行のみ |
| オフホワイト | 中立 | 本文、データ |

**厳守事項**

- 端末緑を見出し・ラベル・装飾に使わない（**情報量のない緑は禁止**）
- 端末緑で長文を組まない（1画面につき最大2〜3行）
- 緑の行には必ず `>` を付ける（色以外でも話者が判別できるように）
- **`profile.html` では端末緑を一切使わない**（本人が直接話す唯一のページ）

例外1件：ブラウンのSVG内の目に `#7FE3A8` を使用。
ブラウンは端末そのものであるため、画面が緑に光るのは設定と一致する。

### 5.2 書体（3種・変更禁止）

| 用途 | 書体 | ライセンス |
|---|---|---|
| 英数・UI・数値 | JetBrains Mono | SIL OFL 1.1 |
| 日本語本文 | BIZ UDPGothic | SIL OFL 1.1 |
| 見出し・ロゴ | DotGothic16 | SIL OFL 1.1 |

- **DotGothic16 の使用面積は画面全体の10%以下。本文に使用禁止**
- 読み込むウェイトを増やさない（表示速度に直結）
- **フォントを追加した場合、必ず `license.html` に追記すること**

### 5.3 形状・レイアウト

| 項目 | 値 |
|---|---|
| 角丸 | **0px（全要素）** |
| 影 | **禁止** |
| グラデーション | **禁止**（走査線のみ例外。デフォルトOFF） |
| 最大コンテンツ幅 | 960px |
| 本文カラム | 680px |
| 右ペイン | 280px（タブレット240px） |
| スペーシング | 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64px **のみ** |
| 本文 | 16px / 行間1.9 |
| 最小文字サイズ | 11px |

### 5.4 演出

| 演出 | 初期値 |
|---|---|
| 走査線 | **OFF**（設定でON可） |
| タイプライター | **OFF**（ON時もクリックで全文即表示） |
| 高コントラスト | OFF（設定でON可） |
| ページ遷移アニメーション | **なし** |
| hover トランジション | **80ms**（超えない） |
| スクロール連動・パララックス | **実装禁止** |

`prefers-reduced-motion: reduce` で全アニメーション無効化。
**モバイル（767px以下）では演出を強制OFF。**

### 5.5 構造

- トップページの選択肢は**6項目固定**。増やさない
- 選択肢に**連番を付けない**（順序ではないため）
- トップページに**上部ナビゲーションを置かない**（下層ページのみ）
- 書類棚のファイル名は**内容が分かる命名を維持**する
  （例：`analysis_001_情報が足りないと面白いのか.txt`）
  → **`analysis_001.txt` のような一般名に戻さないこと。ここがこのサイト最大の発明**
- PDFリンクに **`download` 属性を付けない**（ブラウザで開かせる）
- リンク文言は動作を明示する（「企画書を読む（PDF）」）。
  **「詳しくはこちら」「Click here」は禁止**
- **機能しないダミーボタンを置かない**

### 5.6 パフォーマンス予算

| 指標 | 上限 |
|---|---|
| トップページ初期表示 | **2.0秒**（Fast 3G相当） |
| 画像1枚 | 300KB |
| ページ総転送量 | 1.5MB |
| JavaScript 合計 | 30KB（圧縮後） |
| PDF 1本 | 5MB |

### 5.7 権利・プライバシー

- **他社IPの画像・ロゴ・キャラクター・スクリーンショットを使用しない**
- 既存の製品・サービスの見た目を再現しない（トレードドレスの問題）
- 素材を追加したら**必ず `license.html` に追記**
- **住所・電話番号・生年月日をサイトに載せない**
- 訪問者の情報を取得する仕組みを追加しない

---

## 6. 作業別プロンプト集

依頼するときは、この文書と対象ファイルを添付したうえで、以下をコピーして使ってください。

### 6.1 コンテンツを追加する（企画書・分析・ボツ企画）

```
添付の「ポートフォリオ改修ガイド」と既存ファイルに従って、
新しい{企画書／分析／ボツ企画}ページを1つ追加してください。

【元にするファイル】
{proposal-001.html / analysis-001.html / recovered-001.html} を複製して中身を差し替える

【内容】
（ここに原稿を貼る）

【守ること】
- 既存ページとフィールドの順序・見出し文言・図の位置を1文字も変えない
- style.css に新しいクラスを追加しない（既存の .doc / .spec / .figure / .actions を使う）
- 書類棚（.files）に新しい行を追加し、全ページで同じ内容にそろえる
- ファイル名は内容が分かる命名にする
- 本文中の数値・人数・秒数を勝手に変更しない

【報告してほしいこと】
- style.css を変更したか。した場合はその理由
- 既存ページで内容がずれた箇所があるか
```

### 6.2 デザインを修正する

```
【修正依頼】
対象：{ファイル名} の {該当箇所}
現状：{今どうなっているか}
期待：{どうなってほしいか}
根拠：改修ガイド {章番号}

この修正に関係のない箇所（カラー変数、タイポグラフィ、レイアウト構造、
他ページ）は一切変更しないでください。
変更が必要だと判断した場合は、実装せずに先に説明してください。

修正後、改修ガイド第7章のチェック項目を実行し、結果を報告してください。
```

### 6.3 差し替え作業（ブラウン・アイコン・氏名・写真）

```
以下の差し替えを行ってください。**差し替え以外の変更を一切しないでください。**

対象：{ブラウンのSVG / メニューアイコン6点 / 氏名 / 顔写真}
新しい素材：（添付）

【守ること】
- SVGを差し替える場合、viewBox と表示サイズを既存と同じに保つ
- 塗りの様式を既存とそろえる（線画なら線画、ドット絵ならドット絵）
- アイコンは6点すべて同じ様式にする（1点だけ塗りつぶしにしない）
- 氏名を変更する場合、全ページ（footer含む）で漏れなく置換する
- 素材の出所が変わる場合、license.html の表も更新する
```

### 6.4 不具合を直す

```
【不具合】
症状：{何が起きているか}
再現手順：{どうすると起きるか}
環境：{ブラウザ / 画面幅 / OS}

原因を特定してから修正してください。
症状を隠すだけの対処（!important の追加、要素の非表示など）は避けてください。

修正前に、原因の説明をお願いします。
```

### 6.5 リファクタリング（慎重に）

```
{対象} のコードを整理してください。

【絶対条件】
- 見た目を1ピクセルも変えない
- 挙動を変えない
- カラー変数・スペーシング値・書体を変えない

整理の前後でスクリーンショットを比較し、差分がないことを確認して報告してください。
差分が出た場合は、変更を戻してから報告してください。
```

---

## 7. 検証（作業後に必ず実行）

### 7.1 コマンドによる自動チェック

```bash
# 禁止事項の検出（すべて 0 であること。gradient は走査線の1件のみ許容）
grep -c "border-radius" *.html *.css
grep -c "box-shadow"    *.html *.css
grep -c "download="     *.html
grep -c "localStorage\|sessionStorage" *.html *.js

# 使用色の洗い出し（確定6値＋hc用4値以外が出たら違反）
grep -oh "#[0-9A-Fa-f]\{6\}" *.html *.css | sort | uniq -c | sort -rn

# profile.html に端末緑が無いこと（0 であること）
grep -c "7FE3A8\|brown-say\|brown-note" profile.html

# 絶対パスが残っていないこと
grep -o 'href="/[^"]*"' *.html
```

### 7.2 AIが仕様を破りやすい6箇所

実装が上がってきたら、まずここを見てください。**経験上、この6つが最も崩れます。**

| # | 崩れやすい箇所 | 確認方法 |
|---|---|---|
| 1 | 角丸が付く | `border-radius` を全文検索 |
| 2 | 影・グラデーションが入る | `box-shadow` `gradient` を全文検索 |
| 3 | 緑が装飾に使われる | `--term` `7FE3A8` の使用箇所を全確認 |
| 4 | hover にフェードが付く | `transition` が80msを超えていないか |
| 5 | 新しい色が増える | ハードコードされた `#` を全文検索 |
| 6 | CTA が塗りつぶされる | `.cta` に `background` が入っていないか |

### 7.3 目視チェック

- [ ] 表示領域の高さ650pxで、6ボタン全てが見えている
- [ ] 390px幅で横スクロールが発生しない
- [ ] キーボードのTabだけで全リンクに到達でき、フォーカスリングが見える
- [ ] PDFがダウンロードされずブラウザで開く
- [ ] 演出が全てデフォルトOFF
- [ ] タイプライターON時、クリックで全文が即表示される
- [ ] 上部バーの全ボタンが機能する
- [ ] 書類棚のファイル名が内容の分かる名前のまま
- [ ] 全ページで書類棚の内容が一致している

### 7.4 30秒動線テスト（大きな改修のあと）

未経験者5人に「30秒で企画書を開いてください」と依頼して計測する。

| 経過 | 画面に出ているべきもの |
|---|---|
| 0〜2秒 | ロード完了。氏名・志望職種・ブラウン |
| 2〜10秒 | 6つの選択肢＋書類棚 |
| 10〜30秒 | 企画書のタイトル・1行コンセプト・図が表示済み |

**5人中4人が30秒以内に到達すること。**

---

## 8. 依頼してはいけないこと

| 依頼 | なぜ危険か |
|---|---|
| **「いい感じにして」「モダンにして」** | 制約が全て上書きされ、初期値のデザインに戻される |
| **「参考サイトのように」** | 他社デザインの模倣は権利面でも危険 |
| **一度に全ページの改修** | 細部が必ず落ちる。1〜2ページずつ依頼する |
| **他社IPの画像生成・使用** | 依頼自体をしない |
| **「もっと凝った演出を」** | 視覚的複雑性が上がり、17msの判定で不利になる（3.2） |
| 範囲を指定しない修正依頼 | 無関係な箇所のデザインが勝手に変わる |

### AI生成デザインが初期値として選びがちな型（採用しない）

1. 温かいクリーム地＋高コントラストのセリフ体＋テラコッタ系アクセント
2. ほぼ黒地に蛍光グリーンや朱色1色のアクセント
3. 全要素を同じ角丸カードに切り分け、同じ影を敷く構成
4. トラッキングを広げた全角大文字のラベル
5. 中黒で連結したメタ情報の羅列（A · B · C）
6. リンク文言末尾の矢印（→）
7. スクロール連動のフェードイン、パララックス
8. 順序でないものへの 01 / 02 / 03 の連番

---

## 9. コンテンツ編集ツール（コードに触れずに追加・編集する）

企画書・ゲーム分析・作ったゲーム・ボツ企画・プロフィール・FAQは、
`_edit-tool/` に用意したローカル編集ツールから、コードを一切いじらずに
追加・編集・削除できる。

### 使い方

1. サイトのフォルダ直下にある `編集ツールを起動.bat` をダブルクリックする
2. 数秒後に既定のブラウザで編集画面が自動で開く（開かない場合は
   `http://localhost:4173/editor.html` を手動で開く）
3. 左側のタブから種類を選び、「＋ 新規追加」または「編集」で内容を入力する
4. 「保存する」を押すと、サイトのHTMLファイルが自動で作り直される
   （書類棚・最近の更新・関連ページのリンクなど、全ページの整合性も
   このとき自動で揃う）
5. 画面右上の「サイトを確認する」から、更新後のサイトをその場で確認できる
6. 編集が終わったら、`編集ツールを起動.bat` を実行した黒いウィンドウを閉じる

### できること

- 文章の追加・編集（見出し＋本文のブロックを自由に追加できる）
- 画像の追加（企画書・分析の図版、作ったゲームのサムネイル、プロフィール写真）
- PDFの追加（PowerPointは「エクスポート→PDF/XPSドキュメントの作成」で
  PDF化してからアップロードする。この環境にPDF自動変換の仕組みは
  入っていないため、変換はPowerPoint側の機能を使う）
- 画像追加時に「本人が作成」「生成AIで制作」を選ぶと、`license.html` の
  該当表に自動で行が追加される

### 制限事項・仕様外のこと

- 色・フォント・レイアウト・演出・トップページの6ボタン構成は、この
  ツールからは変更できない（第5章の変更禁止事項のため、意図的に
  フォーム項目を用意していない）。これらの変更は引き続き第6.2章の
  手順で依頼すること
- 最初から入っていたサンプルページ（proposal_001・analysis_001など）を
  このツールで編集すると、図版内の色付き数値強調など一部の装飾は
  失われる（数値・文章そのものは残る）
- 保存のたびに、上書き前のファイルが `_edit-tool/backups/` に日時付きで
  自動退避される。誤って削除・上書きした場合はここから戻せる
- `_edit-tool/` フォルダ自体は `docs/` の外にあるため、GitHub Pagesで
  公開しても一般には配信されない（第10章）

---

## 10. GitHubで公開する（無料・独自ドメインなし）

GitHub Pagesという無料の仕組みで、このサイトをそのままインターネットに公開できる。
公開対象は `docs/` フォルダの中身だけで、`_edit-tool/` は含まれない。

### 初回の公開手順

1. **github.com でリポジトリを作る**（本人のGitHubアカウントで）
   - 右上の「＋」→「New repository」
   - Repository name を決める（例：`shinya-yugi-portfolio`）
   - Public（公開）を選ぶ。README等の追加チェックは入れずに「Create repository」
2. **できたリポジトリのURL**（例：`https://github.com/ユーザー名/リポジトリ名.git`）を控える
3. AIプログラマー、または本人が以下を実行してこのフォルダを紐づけ、送信する
   ```bash
   git remote add origin <控えたURL>
   git branch -M main
   git push -u origin main
   ```
4. GitHub側の画面で「Settings」→左メニュー「Pages」を開く
5. 「Build and deployment」の「Branch」で `main` を選び、フォルダを `/docs` に設定して「Save」
6. 数分待つと、同じ画面に公開URL（`https://ユーザー名.github.io/リポジトリ名/`）が表示される

### 2回目以降、内容を更新したとき

編集ツールで保存すると `docs/` 内のファイルが書き換わるだけで、GitHub側は自動更新されない。
反映するには、変更後に以下を実行して送信し直す必要がある。
```bash
git add docs
git commit -m "内容を更新"
git push
```
（この一連の操作は、AIプログラマーに「GitHubに反映して」と頼めば代行できる）

### 注意

- リポジトリをPublicにすると、`docs/` の中身は誰でもソースコードごと閲覧できる
  （見た目の公開範囲は今までのURLと同じで、追加で個人情報が漏れるわけではない）
- 独自ドメインを取得すればこの `.github.io` のURLの代わりに使えるが、それは別タスク（第11章 #8）

---

## 11. 未完了のタスク

| # | タスク | 状態 |
|---|---|---|
| 1 | 氏名を仮名から本名へ差し替え | 未 |
| 2 | ブラウンをデザイナー版に差し替え | 未（現在は生成AI版） |
| 3 | メニューアイコン6点をデザイナー版に差し替え | 未（現在は生成AI版） |
| 4 | 顔写真の掲載 | 未 |
| 5 | 企画書PDFの実ファイル配置（`assets/pdf/`） | 未（第9章の編集ツールからアップロード可） |
| 6 | 企画書2本目・分析2本目・ボツ企画2〜3件の追加 | 未（第9章の編集ツールから追加可） |
| 7 | 遊べるゲームの実リンク接続 | 未 |
| 8 | 独自ドメインの取得と本番デプロイ | 未 |
| 9 | 30秒動線テスト（5人） | 未 |
| 10 | 既存ページの本文を新しい芯（第2章）に合わせて書き直す | 未（着手中。企画書・分析・ボツ企画の「何を選ばせるか」論拠を「何を感じてほしいか」に、プロフィールの趣味の話を心に残ったゲーム体験の気づきに差し替える） |

### 差し替え時の注意（2・3について）

- アイコンは**6点すべて同じ様式**にすること。1点だけ塗りつぶしが混ざると密度が不均衡になる
- ブラウンは**フラットな様式**を維持すること。陰影・グラデーションのある立体イラストは、
  角丸0・影なしというサイト仕様と衝突する
- 差し替えたら **`license.html` の表を必ず更新**すること

---

## 12. この文書と実装が矛盾したとき

**この文書が優先します。**
実装の数値は目的を達成するための手段であり、目的そのものではありません。

ただし、矛盾を見つけたら**作業を止めて報告**してください。
**黙って解釈で埋めないこと。** 報告を受けたら、両方を更新します。

---

## 参照元

- Nielsen Norman Group「How Long Do Users Stay on Web Pages?」「How Little Do Users Read?」
- Lindgaard, G. et al. (2006) *Behaviour & Information Technology*, 25(2), 115–126
- Tuch, A.N. et al. (2012) "The role of visual complexity and prototypicality regarding first impression of websites" *International Journal of Human-Computer Studies*
- Hekkert, P. et al. (2003) *British Journal of Psychology*, 94(1), 111–124
- Silvennoinen, J. et al. (2025) *International Journal of Human–Computer Interaction*
- von Restorff, H. (1933) 孤立効果
- Ladders, Inc. "Eye-Tracking Study" (2018)
- GDKeys「Tips to Apply for a Game Designer Position」


## 2026-09-15 トップ構成の変更
所有者の依頼により、ゲーム画面→作品一覧→企画・分析→プロフィールの順に変更。従来の6ボタン・書類棚優先の指定に優先する。深夜紺・水色・ブラウンは継続。編集ツールの「作ったゲーム」のサムネイル・紹介文・プレイURL・制作メモURLをトップにも反映する。画像のある最初の作品（なければ最初の作品）をピックアップする。制作メモURLには企画書ページも指定可能。未登録の画像・プレイURLは準備中と表示する。


## 2026-09-15：プロフィールを最初に表示（最新方針）
「自分を覚えてもらう」を第一のコンセプトとする。トップは氏名・志望職種・所属・制作への思い・ブラウン（写真登録時は写真も）→ゲーム画面→ゲーム一覧→企画・分析の順。直前のゲーム最優先の配置指示に優先する。プロフィールの長文は詳細ページに置き、作品にもすぐ進めるようにする。編集ツールのプロフィールを変更するとトップにも反映される。


## サイト名の更新
サイト名は「Poro」。氏名・制作への思いを最初に見せ、自分を覚えてもらう方針は継続。名称の由来は未設定のため創作しない。
